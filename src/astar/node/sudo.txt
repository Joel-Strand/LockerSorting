out = [xMax,yMax,zMax]
arrayFromFile n = '...'
int x, y, z, counter = 0;

bool flag = true;

while (flag) {
    if (n[counter] == null) {
        flag = false;
        return;
    }
    if (n[counter] == 4 || n[counter+1] == 4)
        {z++;}
    if (n[counter] == 4)
        {y++;}

   out[x,y,z] = n[counter] (parse int?)
   x++;
   counter++;
}

